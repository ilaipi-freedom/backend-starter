/// 可用状态
enum AvailableStatus {
  normal /// 正常
  forbidden /// 禁用
}

enum SysDictCategory {
  radio
  select
  text
  number
  date
  datetime
  boolean
}

enum SysMenuType {
  CATALOG // 目录
  MENU // 菜单
  BUTTON // 按钮
}
