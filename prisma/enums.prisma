/// 可用状态
enum AvailableStatus {
  normal /// 正常
  forbidden /// 禁用
}

enum SysDictCategory {
  radio
  select
  text
  number
  date
  datetime
  boolean
}

enum SysMenuType {
  catalog // 目录
  menu // 菜单
  button // 按钮
  embedded // 内嵌
  link // 链接
}
